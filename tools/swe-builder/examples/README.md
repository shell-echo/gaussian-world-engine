# swe-builder examples

Use the existing public capture session as a starting point:

```text
public/captures/outdoor-loop/session.json
```

A local capture project generated by the CLI will contain the same contract, plus writable folders for plans and large-world exports.
